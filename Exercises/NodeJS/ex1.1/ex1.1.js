// 1. node.js can have access to the file system, 
// because it's binded to C++ functions, and
// C++ has this access.

// 2. node.js can run outside of the browser.

// 3. node.js can use the "global" object
// and the "process" object.


// 1. JS can run in the browser

// 2. JS has the "window" object and the
// "DOM" object

// 3. JS can add HTML tags.
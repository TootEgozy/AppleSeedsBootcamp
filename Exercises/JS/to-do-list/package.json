{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "1. create a class ToDoList with methods:\r    createNewTask (recives a string, creates a task with an id (incrementing number), string(user input), isCompeleted (boolean - defalt false)),\r    \\*\\*\\* function to automatically add each new task, if not empty, into storage tasksList",
  "main": "to-do-list.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
